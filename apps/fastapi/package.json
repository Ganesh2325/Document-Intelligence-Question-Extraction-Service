{
  "name": "@folio/fastapi",
  "version": "1.0.0",
  "private": true,
  "description": "FastAPI layer for Folio document processing and question extraction",
  "scripts": {
    "dev": "python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 3001",
    "start": "python -m uvicorn app.main:app --host 0.0.0.0 --port 3001",
    "test": "python -m pytest -q"
  }
}
